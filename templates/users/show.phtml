<table border="solid 1px">
    <tr>
        <th>Name</th>
        <th>Email</th>
        <th>City</th>
    </tr>
    <?php foreach ($users as $user): ?>
    <tr>
        <td><?= "{$user['name']}" ?></td>
        <td><?= "{$user['email']}" ?></td>
        <td><?= "{$user['city']}" ?></td>
        <td><a href="user/<?= $user['id'] ?>/edit">Edit</a></td>
    </tr>
    <?php endforeach ?>
</table>